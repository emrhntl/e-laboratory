import AnalysisValue from "./analysisValue";

export default class Analysis {
    constructor(
        analysisId:string,
        userId:string,
        guideId:string,
        values: AnalysisValue
    ) { }
}