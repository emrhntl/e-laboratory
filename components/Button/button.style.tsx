import { StyleSheet } from "react-native";

export default StyleSheet.create({
    button: {
      padding: 17,
      borderRadius: 25,
      width: '100%',
      alignItems: 'center',
    },
    buttonText: {
      fontWeight: 'bold',
      fontSize: 16,
    },
  });